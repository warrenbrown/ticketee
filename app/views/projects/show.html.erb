<% title(@project.name, "Projects") %>

<h1>Project <%= @project.name %></h1>
<%= link_to 'Edit Project', edit_project_path(@project) %>
<%= link_to 'Delete Project', project_path(@project), method: :delete, data: { confirm: 'Are you really sure you want to delete this project?' } %>
<p>
  <%= @project.description %>
</p>
